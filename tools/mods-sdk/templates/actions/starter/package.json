{
    "name": "my-action-mod",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "mods-sdk build",
        "build:dev": "mods-sdk build --watch --debug",
        "tsc:dev": "tsc --watch",
        "server": "mods-dev-server --allow-project-root true"
    },
    "devDependencies": {
        "@spotfire/mods-api": "~MODS-API-VERSION",
        "@spotfire/mods-dev-server": "^1.4.0-preview.0",
        "@spotfire/mods-sdk": "^MODS-SDK-VERSION",
        "esbuild": "^0.28.1",
        "prettier": "^3.8.1",
        "typescript": "^5.9.3"
    },
    "engines": {
        "node": ">=22.12.0"
    },
    "allowScripts": {
        "esbuild": true
    }
}
