{
    "name": "my-action-mod",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "mods-sdk build",
        "build:dev": "mods-sdk build --watch --debug",
        "tsc:dev": "tsc --watch",
        "server": "mods-dev-server --allow-project-root true"
    },
    "devDependencies": {
        "@spotfire/mods-api": "~MODS-API-VERSION",
        "@spotfire/mods-dev-server": "^1.2.0-preview.0",
        "@spotfire/mods-sdk": "^MODS-SDK-VERSION",
        "esbuild": "^0.25.1",
        "prettier": "^3.3.0",
        "typescript": "^5.8.2"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}
